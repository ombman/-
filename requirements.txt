# REINS自動検索アプリ 依存ライブラリ
# playwright : ブラウザ（Chrome）を自動操作するためのライブラリ
# keyring    : ID/パスワードをWindows資格情報マネージャーに安全に保存する
#
# ※バージョンは「以上」で指定しています。新しいPython(3.12〜3.14など)でも、
#   それに対応した新しい版が自動で入るようにするためです。
playwright>=1.47,<2
keyring>=25,<26
